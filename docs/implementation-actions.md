# Implementation Actions

Maintained per the format defined in [.claude/product-architect.md](../.claude/product-architect.md).
Detailed milestone descriptions live in the vault's `Project Overview.md` roadmap table
(`OV-TheInterviewAgent/TheInterviewAgent/Project Overview.md` — `docs/architecture/overview.md`
is retired, do not use it) — this list is the actionable, trackable version, split into
required work and optional improvements, plus near-term gaps surfaced by the risk register.

## Required

| ID | Action | Reason | Owner | Priority | Dependencies | Acceptance criteria | Related decision | Status |
|---|---|---|---|---|---|---|---|---|
| IA-001 | Wire `ai_processing_logs` writes into `llm_client.py`, `stt_client.py`, `tts_client.py`, `resume_parser.py` | Cost-sensitive POC has zero actual cost tracking today (R-008) | Amit Tiwari | High | None | Every AI call writes a row with model, token/duration counts, and cost | PD-002, R-008 | Not started |
| IA-008 | Address PII/compliance gap: encryption at rest/in transit, data deletion path | PRD §6 requires this; currently plaintext local storage (R-006) — now covers both résumés and interview audio (M4 shipped audio storage unencrypted, deliberately risk-accepted per ADR-008 rather than deferred silently — see R-006) | Amit Tiwari | High (before real candidate data) | IA-007 | Resume files and interview audio both encrypted at rest; a deletion endpoint/process exists | R-006, ADR-008, PRD §6 | Not started |

## Optional / improvements

| ID | Action | Reason | Owner | Priority | Dependencies | Acceptance criteria | Related decision | Status |
|---|---|---|---|---|---|---|---|---|
| IA-010 | Rebuild `ivfflat` indexes on `resumes.embedding`/`skills.embedding` after real embeddings are populated | Indexes were built empty; pgvector guidance says index quality depends on representative data at build time (R-007) | Amit Tiwari | Low | Whichever milestone first populates embeddings in bulk | `REINDEX` run and documented after first bulk embedding population | ADR-003, R-007 | Not started |
| IA-011 | M6b: deploy to Cloud Run + Neon + GCS | Currently local-only, no CI/monitoring (R-009) | Amit Tiwari | Low (deferred by design) | M6 (auth) should land first | App reachable outside localhost with cost within the stated free/cheap-tier target | PD-002, R-009 | Not started |

## Done (for traceability)

| ID | Action | Reason | Owner | Priority | Dependencies | Acceptance criteria | Related decision | Status |
|---|---|---|---|---|---|---|---|---|
| IA-000a | Adopt full 9-table ATS schema with pgvector | Avoid a second schema migration once M2/M3/M5 need it | Amit Tiwari | High | None | All 9 tables, both extensions, and every specified index exist and verified via `\dt`/`\dx`/`\di` | ADR-003 | Done |
| IA-000b | Build and validate the STT→LLM→TTS cascade as a standalone script | De-risk the voice pipeline before any app/DB integration | Amit Tiwari | High | None | Two-turn scripted demo runs end-to-end with coherent multi-turn output, verified by listening to generated audio | ADR-004 | Done |
| IA-000c | Log PD-001 and PD-002 | Formalize product decisions that were previously only stated in conversation | Amit Tiwari | Medium | None | PD files exist under `docs/product-decisions/` following the required template | PD-001, PD-002 | Done |
| IA-005 | M3: AI question generation (8–12 Qs, edit/reorder/regenerate) | Core PRD feature (§5.2) | Amit Tiwari | Medium | M2 (partial — deterministic scoring was sufficient) | Recruiter can generate, edit, reorder, and regenerate individual questions via the API, with optional per-candidate personalization | PRD §5.2 | Done (2026-08-10) |
| IA-007 | M6 Phase 1/2: tenant isolation + RBAC enforcement | No authentication existed anywhere; blocked any real multi-user usage (R-001) | Amit Tiwari | High | None | Every endpoint tenant-scoped and role-checked; leak + role-matrix tests passing | R-001, PRD §5.5/§6, [[Identity & Access Overview]] | Done (2026-08-09) — Phase 3 (real tenant-user SSO) and the master admin module's own scope are tracked separately, not by this action |
| IA-013 | Establish incremental commit discipline — commit the current working-tree state as a checkpoint, then one commit per feature going forward | Repository has one commit total despite three shipped features sitting uncommitted (R-012); every ADR's rollback strategy assumes git history that doesn't exist | Amit Tiwari | Medium | None | Working tree is clean; subsequent features land as their own commits, not accumulated indefinitely | R-012 | Done (2026-08-10) — checkpoint commit `ef827e3` (87 files) pushed to `origin/main`; historical single-commit gap accepted as-is, discipline applies going forward |
| IA-012 | Add a `CHECK` constraint preventing an `Interview` row from having both `shared = true` and `candidate_id IS NOT NULL` | M3 shipped candidate personalization without a guard against sharing a personalized interview with other candidates (R-011) | Amit Tiwari | Medium | None | A migration adds the constraint (`ck_interviews_no_shared_personalized`); the API returns a clean 409, not a raw `IntegrityError` 500, with a test | R-011 | Done (2026-08-10) — migration `f6a7b8c9d0e1`, `PATCH /interviews/{id}` catches the `IntegrityError` and returns 409, `test_sharing_a_personalized_interview_is_rejected` passing |
| IA-014 | Share one connection-pooled `httpx.AsyncClient` across `stt_client.py`/`llm_client.py`/`tts_client.py` instead of each opening its own per call | Each cascade turn currently opens three separate TCP/TLS handshakes to `openrouter.ai` — a free latency win independent of ADR-007's transport decision (found during ADR-007's second pass) | Amit Tiwari | Medium | None | One shared client instance reused across a turn's three calls | ADR-007 | Done (2026-08-10) — `llm_client.get_http_client()` is the shared singleton; `stt_client.py`/`tts_client.py` updated to use it; live smoke-tested against the real API to confirm client reuse |
| IA-015 | Name M4's new schema `interview_sessions`/`interview_turns`, not `sessions` | `sessions` is already the admin-auth login-session table (`app/models/session.py`) — reusing the name collides in both schema and mental model (found during ADR-007's second pass) | Amit Tiwari | Low | None | M4's migration uses a name that doesn't collide with the existing `sessions` table | ADR-007 | Decided (2026-08-10) — `interview_sessions`/`interview_turns` confirmed; tables don't exist yet, created when IA-006 (M4) actually builds them |
| IA-002 | Measure end-to-end latency of one full cascade turn (STT+LLM+TTS) | ADR-004 left this as an explicit unresolved question; blocks knowing if async is even fast enough per PRD §6 | Amit Tiwari | High | IA-014 | 3 real runs of `scripts/test_interview_pipeline.py` (n=2 turns each): full-turn totals 8.24s and 12.51s on the clean run — comfortably under ADR-007's ~25–30s ceiling. Per-leg: STT ~1s, LLM 2.4–5.5s, TTS 4.9–5.9s (TTS dominates, not LLM). **But real variance/reliability findings, not just a happy-path number**: one earlier run's TTS call hit the full 60s timeout with no response at all; another run's LLM call returned a 200 with no usable `choices` field, crashing with an unhandled `KeyError` — a live reproduction of the long-flagged `llm_client.py` response-validation gap (see Backend Overview/Project Overview D1), fixed in the same session as a direct blocker to completing this measurement. n=2 clean turns is not a real p50/p95 — this is a first grounded number, not a final one. | ADR-004, ADR-007 | Done (2026-08-10) — see full write-up above; **elevates R-004** from "not tested" to observed, and fed a real bug fix back into `llm_client.py` |
| IA-009 | Add automatic LLM fallback (DeepSeek V4 Pro) if the free-tier interview model fails, plus retry for STT/TTS | No fallback existed; IA-002 turned "no fallback exists" from theoretical (R-003, R-004) into observed — a live 60s TTS timeout and a malformed LLM response both happened in 3 short runs | Amit Tiwari | **Bumped High** (was Low — reconsidered per IA-002's observed evidence, not just planned) | IA-002, IA-014 | `chat_completion` gained an opt-in `fallback_model` param — on primary failure (timeout, network error, non-200, or malformed shape) it retries once against a different model; only the interview cascade (`interview_pipeline.py`) passes one (`interview_llm_fallback_model` = `deepseek/deepseek-v4-pro`), every other caller (question generation, résumé parsing) is unaffected. `stt_client.py`/`tts_client.py` get one same-model retry with backoff — no second model exists on record for either, so a model swap isn't the honest fix there, a retry is. 8 new tests (`test_ai_client_resilience.py`) drive a fake HTTP client double to exercise the real retry/fallback code, not mocked-away business logic; live-verified end-to-end against the real API too. **Known limit, stated not hidden**: this catches hard failures, not "slow but successful" responses — the same live run that verified this wiring also hit a real 24.63s *successful* LLM call, which the fallback correctly does not touch (it isn't a failure) — a performance guarantee would need a different, bigger design (a race against a shorter timeout), not built here. | ADR-002, R-003, R-004 | Done (2026-08-10) |
| IA-003 | Move resume scoring (and eventually parsing) off the request path (M2: BackgroundTasks + polling) | Inline AI calls in the request path will degrade/timeout as more AI work is added (R-002) | Amit Tiwari | High | None | `POST` returns immediately with a pending status; a polling `GET` returns the result once ready | R-002 | Done (2026-08-10) — `POST /candidates/{id}/judge` returns `202` via FastAPI `BackgroundTasks` (this codebase's first use of it); `judge_status`/`judge_error` columns (migration `b8c9d0e1f2a3`) make the result pollable via the existing `GET /candidates/{id}`. Scope deliberately narrow — only the LLM-judge call, not question generation or résumé parsing, which stay synchronous (R-002 remains open for those) |
| IA-004 | Add a conversation-history bound (truncation, summarization, or sliding window) to `interview_pipeline.py` | History currently grows unboundedly, risking rising cost/latency on long interviews (R-010) — externalizing storage to Postgres (ADR-007) does not by itself bound this | Amit Tiwari | Medium | None (IA-002's latency baseline is now in) | A defined max history size/strategy, with a test showing a long interview doesn't grow context unboundedly | ADR-004, ADR-007, R-010 | Done (2026-08-10) — `bound_history()` in `interview_pipeline.py` keeps the system prompt + last `interview_history_max_turns` messages (config-driven, default 12); pure function, called by the M4 router before each `run_turn`, per ADR-007's requirement that `run_turn`'s own signature not change. Covered by `test_history_bounding_caps_messages_sent_to_llm` |
| IA-006 | M4: wire the voice cascade into the real app with DB persistence | Currently only a standalone script (`scripts/test_interview_pipeline.py`); not usable by an actual candidate | Amit Tiwari | Medium | IA-002, IA-004 | Per ADR-007: a synchronous endpoint persists each interview turn to Postgres for the asynchronous-recording mode first; a WebSocket-streamed variant only once the live-moderated mode is scheduled. Not "Celery+Redis," per ADR-007's finding that a task queue is the wrong shape for a live per-turn conversation | ADR-004, ADR-007, ADR-008 | Done (2026-08-10) — new `interview_sessions`/`interview_turns` tables (migration `c9d0e1f2a3b4`), new `app/routers/interview_sessions.py` (session creation, persist-before-calling turn submission with `(session_id, turn_index)` idempotency, reload/resume `GET`). Voice mode only, scoped deliberately — Chat/Avatar untouched. The interviewer now asks the interview's own M3-curated questions in order (not open-ended) and signals completion via a detectable sentinel, closing a gap ADR-007 never addressed. Candidate auth and audio-storage decisions recorded separately in ADR-008 (both were explicitly **not** blockers for this action, once decided — a fast pre-implementation `/architect-review` surfaced them before any code was written, not after). Live-verified against the real OpenRouter API end-to-end, including a real TTS timeout hitting the failed→retry path exactly as designed. 12 new tests (`test_interview_sessions.py`), full suite 82/82 passing |
